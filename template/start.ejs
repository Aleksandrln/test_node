<html>
<head>
    <style type="text/css">
        .btn {
            font-size: 58px;
            font-family: Verdana;
            padding: 60px;
            border-radius: 20px;
            background: -moz-linear-gradient(top, #68a 0%, #246);
            background: -webkit-gradient(linear, left top, left bottom, from(#68a), to(#246));
            background: -o-linear-gradient(top, #68a 0%, #246);
            color: #fff;
            margin-right: 20px;
        }

        .container {
            position: absolute;
            top: 50%;
            left: 50%;
            margin: -27px 0 0 -306px;
        }
    </style>
</head>
<body>
<div class="container">
    <a href="<%= href %>" class="btn">Авторизоваться</a>
</div>
</body>
</html>